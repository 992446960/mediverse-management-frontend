---
description: 执行计划文档时，根据「执行指令」调用对应 subagent
globs: doc/前端开发文档/plans/**/*.md
alwaysApply: false
---

# 计划执行与 Subagent 调用

当用户要求**执行**或**实现** `doc/前端开发文档/plans/` 下的计划文档时，主 Agent 必须：

1. **检查计划文档**是否包含「执行指令」小节（通常在「二、参与的智能体与 Skills」下）
2. **若存在执行指令**且指定了 `subagent_type`：
   - 使用 `mcp_task` 工具调用对应 subagent
   - 将 `subagent_type` 设为执行指令中指定的值（如 `frontend-developer`）
   - 将计划全文或至少「三、计划内容」中的任务清单作为 `prompt` 传入
   - 执行前确认前置依赖已完成
3. **若无执行指令**：主 Agent 可自行实现，或询问用户是否委托 subagent 执行

## 执行指令格式示例

计划文档中的执行指令通常形如：

```markdown
### 执行指令（主 Agent 必读）

执行本计划时，**必须**通过 `mcp_task` 调用 subagent：
- **subagent_type**: `frontend-developer`
- **prompt**: 本计划全文，或至少包含「三、计划内容」中的任务清单
- **前置条件**: ...
```

## 调用示例

```text
mcp_task(
  subagent_type: "frontend-developer",
  prompt: "<计划全文或任务清单>",
  description: "实现 Plan 09-01 文件预览页面"
)
```
