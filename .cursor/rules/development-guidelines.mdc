---
description: 前端开发规范 — 编写或修改 src/ 下的代码时自动生效，按需查阅规范文档对应章节
globs: src/**/*.{vue,ts,tsx,css}
alwaysApply: true
---

# 前端开发规范（按需查阅）

本项目有完整的开发规范文档：`doc/前端开发文档/项目开发规范.md`。

## 核心规则速查（无需读取文档）

1. **SFC 顺序**：`<template>` → `<script setup lang="ts">` → `<style scoped>`
2. **自动导入**：`ref`/`computed`/`watch`/`onMounted` 等 Vue API 已自动导入，禁止 `import { ref } from 'vue'`
3. **Antd 组件**：模板中用 `<a-button>` 等标签，已自动按需导入，禁止 `import { Button } from 'ant-design-vue'`（`message`/`Modal.confirm` 等函数式 API 除外）
4. **i18n 强制**：所有用户可见文本必须用 `t('key')`，禁止硬编码中文/英文
5. **弹窗 prop**：统一用 `v-model:open`，禁止废弃的 `visible`
6. **组件导入路径**：`import Xxx from '@/components/Xxx/index.vue'`，禁止目录导入
7. **类型导入**：用 `import type { ... }` 与值导入分开
8. **CSS 颜色**：用 `var(--color-xxx)` CSS 变量，禁止硬编码色值
9. **公共组件**：禁止随意修改 `src/components/` 下公共组件；优先用配置/Props 扩展，确需修改时评估影响并说明

## 按需查阅索引

当任务涉及以下场景时，用 Read 工具读取 `doc/前端开发文档/项目开发规范.md` 对应行范围：

| 场景 | 章节 | 行范围 |
|------|------|--------|
| 新增依赖、确认技术选型 | 一、技术栈与工具链 | 27-56 |
| 创建新文件/目录、文件命名 | 二、目录结构约定 | 57-131 |
| 代码格式、ESLint、自动导入细节 | 三、代码风格 | 132-234 |
| 开发列表页（三件套）、表单弹窗、组件 ref | 四、组件开发规范 | 235-416 |
| 修改公共组件、扩展三件套能力 | 四、组件开发规范（4.5） | 378-386 |
| 表单头像设置（与分身设置一致） | 四、组件开发规范（4.8） | 417-431 |
| 头像/图片预览（统一使用 a-image） | 四、组件开发规范（4.8.1） | 432-442 |
| 创建/修改 Pinia Store | 五、状态管理规范 | 417-477 |
| 编写 API 函数、Mock Handler、错误处理 | 六、API 调用与 Mock | 478-604 |
| 新增 i18n key、翻译文件 | 七、国际化 i18n 规范 | 605-697 |
| 样式编写、CSS 变量、暗色模式、BEM | 八、样式规范 | 698-795 |
| 类型定义、类型文件组织、枚举 | 九、TypeScript 规范 | 796-881 |
| 路由配置、权限守卫、v-permission | 十、路由与权限 | 882-942 |
| Git commit、分支 | 十一、Git 工作流 | 943-989 |
| 提交前自查清单 | 十二、代码质量检查 | 990-1022 |
| Token 管理、XSS 防护 | 十三、安全规范 | 1023-1055 |
| 编写 Composable | 附录 A | 1077-1114 |
| confirmDelete 工具 | 附录 B | 1115-1140 |

**读取示例**：需要开发列表页时 → `Read doc/前端开发文档/项目开发规范.md offset=235 limit=181`

## ESLint 规范

本项目已配置 ESLint，**每次完成代码修改后必须确保无 ESLint 报错**：

- 每次编写或修改代码后，使用 `ReadLints` 工具检查所有被修改的文件
- 若发现 ESLint 错误，必须在本次任务中修复，不得遗留
- 禁止使用 `// eslint-disable` 注释绕过规则，除非有明确说明的特殊原因
- 详细 ESLint 配置规则参见 `doc/前端开发文档/项目开发规范.md`（三、代码风格，行 132-234）

## 使用原则

- **不要一次性读取整个文档**，只在任务涉及特定场景时读取对应章节
- 如果核心规则速查已覆盖，无需读取文档
- 多个场景交叉时可合并行范围一次读取
