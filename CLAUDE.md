# 核心原则

- 必须先读现有实现和 `AGENTS.md`，再修改代码或文档。
- 必须保持开发规范、开发文档、Docker 文档和任务看板与代码同步。
- 禁止提交真实服务器地址、Token、账号密码或 `.env.api-test`。
- 禁止把 `AGENTS.md` 写成文档百科；细节必须放入 `docs/`。
- 交付前必须运行对应验证命令，并说明无法运行的原因。

操作指令与项目详情见 `AGENTS.md`
